import ChatContainer from '../chat/ChatContainer/ChatContainer';

export default function AssistantContent(): React.ReactNode {
    return <ChatContainer className='AssistantContent w-full h-[calc(100%-2.5rem)]' />;
}
